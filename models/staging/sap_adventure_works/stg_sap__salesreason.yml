version: 2

models:
  - name: stg_sap__salesreason
    description: Table with sales reason information.
    columns:
      - name: salesreasonid
        description: Seales reason ID.

      - name: name
        description: Sales reason name.

      - name: reasontype
        description: Sales reason type.

      - name: modifieddate
        description: Modified date
